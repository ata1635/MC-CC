peripheral.find("modem", rednet.open)
rednet.send(3, "start")
rednet.send(4, "start")
rednet.send(6, "start")
rednet.send(8, "start")

while true do
    local id, message = rednet.receive()
    if id == 6 then
        print(message)
    end
end
